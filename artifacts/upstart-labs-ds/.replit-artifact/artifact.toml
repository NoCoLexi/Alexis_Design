kind = "design-system"
previewPath = "/upstart-labs-ds/"
title = "UpStart-Labs Design System"
description = "Visual design system for UpStart-Labs — product strategy and AI adoption consulting. Warm off-white surfaces, Ink text, and a signature orange accent (Newsreader display, DM Sans UI, DM Mono labels)."
version = "1.0.0"
id = "artifacts/upstart-labs-ds"
router = "path"

[[services]]
name = "web"
paths = [ "/upstart-labs-ds/" ]
localPort = 20216

[services.development]
run = "pnpm --filter @workspace/upstart-labs-ds run dev"

[services.env]
PORT = "20216"
BASE_PATH = "/upstart-labs-ds/"
